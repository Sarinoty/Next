import React from "react";

const CategoryPage = () => {
    //throw new Error("Error from category page")
    return (
        <div className="page">
            <h2>Welcome to products page</h2>
        </div>
    );
};

export default CategoryPage;